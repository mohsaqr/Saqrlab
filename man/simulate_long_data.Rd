% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_long_data.R
\name{simulate_long_data}
\alias{simulate_long_data}
\title{Simulate Long Format Sequence Data}
\usage{
simulate_long_data(
  n_groups = 5,
  n_actors = 10,
  n_courses = 3,
  n_states = 9,
  states = NULL,
  use_learning_states = TRUE,
  categories = "group_regulation",
  seq_length_range = c(10, 30),
  achiever_levels = c("High", "Low"),
  achiever_probs = c(0.5, 0.5),
  start_time = "2025-01-01 10:00:00",
  time_interval_range = c(60, 600),
  trans_matrix = NULL,
  init_probs = NULL,
  seed = NULL,
  actors_per_group = NULL,
  actions = NULL,
  action_categories = NULL,
  n_actions = NULL,
  transition_probs = NULL,
  initial_probs = NULL
)
}
\arguments{
\item{n_groups}{Integer. Number of groups. Default: 5.}

\item{n_actors}{Integer or integer vector of length 2. If single
integer, exact number of actors per group. If vector c(min, max), random
sizes in that range. Default: 10.}

\item{n_courses}{Integer or character vector. Number of courses or specific
course names (e.g., c("A", "B", "C")). Groups are distributed evenly
across courses. Default: 3.}

\item{n_states}{Integer. Number of actions to sample when using categories.
Ignored if \code{states} is provided. Default: 9.}

\item{states}{Character vector. The action/state names to use. If NULL,
uses learning states based on \code{categories}. Default: NULL.}

\item{use_learning_states}{Logical. If TRUE and \code{states} is NULL, uses
learning state verbs from the specified \code{categories}. Default: TRUE.}

\item{categories}{Character vector. Categories of learning states
to use when \code{states = NULL}. Options: "metacognitive", "cognitive",
"behavioral", "social", "motivational", "affective", "group_regulation",
or "all". Default: "group_regulation".}

\item{seq_length_range}{Integer vector of length 2. Range for sequence
lengths per actor (min, max). Default: c(10, 30).}

\item{achiever_levels}{Character vector. Levels for the Achiever variable.
Default: c("High", "Low").}

\item{achiever_probs}{Numeric vector. Probabilities for each achiever level.
Must sum to 1. Default: c(0.5, 0.5).}

\item{start_time}{POSIXct or character. Start time for timestamps.
Default: "2025-01-01 10:00:00".}

\item{time_interval_range}{Numeric vector of length 2. Range for time
intervals between actions in seconds (min, max). Default: c(60, 600).}

\item{trans_matrix}{Matrix or NULL. Custom transition probability matrix.
If NULL, generates random probabilities. Default: NULL.}

\item{init_probs}{Numeric vector or NULL. Custom initial probabilities.
If NULL, generates random probabilities. Default: NULL.}

\item{seed}{Integer or NULL. Random seed for reproducibility. Default: NULL.}

\item{actors_per_group}{Deprecated. Use \code{n_actors} instead.}

\item{actions}{Deprecated. Use \code{states} instead.}

\item{action_categories}{Deprecated. Use \code{categories} instead.}

\item{n_actions}{Deprecated. Use \code{n_states} instead.}

\item{transition_probs}{Deprecated. Use \code{trans_matrix} instead.}

\item{initial_probs}{Deprecated. Use \code{init_probs} instead.}
}
\value{
A tibble (data frame) with columns:
\describe{
\item{Actor}{Integer. Actor identifier (1 to n_actors).}
\item{Achiever}{Character. Achievement level (e.g., "High", "Low").}
\item{Group}{Numeric. Group identifier (1 to n_groups).}
\item{Course}{Character. Course identifier.}
\item{Time}{POSIXct. Timestamp of the action.}
\item{Action}{Character. The action/state performed.}
}
}
\description{
Generate simulated sequence data in long format, matching the structure of
\code{group_regulation_long} in the tna package. Creates realistic educational
data with actors nested in groups, groups nested in courses.
}
\details{
The data structure follows a nested hierarchy:
\itemize{
\item \strong{Courses} contain multiple \strong{Groups}
\item \strong{Groups} contain multiple \strong{Actors} (e.g., 10 per group)
\item \strong{Actors} have multiple \strong{Actions} over time
}

This matches the structure of \code{group_regulation_long} from the tna package:
\itemize{
\item Each group has a fixed number of actors
\item Groups are distributed across courses
\item Actors within a group share the same course
}

\strong{Built-in Group Regulation Actions}: When \code{categories = "group_regulation"},
uses the 9 SSRL (Socially Shared Regulation of Learning) actions:
adapt, cohesion, consensus, coregulate, discuss, emotion, monitor, plan, synthesis.
}
\examples{
\dontrun{
# Basic usage: 5 groups, 10 actors each, 3 courses (new defaults)
data <- simulate_long_data(seed = 42)

# Explicit new parameter names
data <- simulate_long_data(
  n_groups = 10,
  n_actors = 15,
  n_states = 6,
  categories = c("metacognitive", "cognitive"),
  seq_length_range = c(15, 40),
  seed = 42
)

# Check structure
length(unique(data$Actor))   # 50 actors
length(unique(data$Group))   # 5 groups
table(table(data$Actor, data$Group) > 0)  # 10 actors per group

# Variable group sizes
data <- simulate_long_data(
  n_groups = 30,
  n_actors = c(8, 12),
  states = c("Read", "Write", "Discuss", "Plan", "Review"),
  seed = 456
)

# Using specific learning state categories
data <- simulate_long_data(
  n_groups = 50,
  n_actors = 10,
  categories = c("metacognitive", "cognitive"),
  n_states = 8,
  seed = 789
)

# Old parameter names still work (backward compatible)
data <- simulate_long_data(
  actors_per_group = 10,
  actions = c("A", "B", "C"),
  seed = 42
)
}

}
\seealso{
\code{\link{simulate_onehot_data}} for one-hot encoded format,
\code{\link{simulate_sequences}} for generating wide-format sequences,
\code{\link{get_learning_states}} for available learning states,
\code{\link{generate_tna_networks}} for generating TNA network objects.
}
