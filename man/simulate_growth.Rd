% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_multilevel.R
\name{simulate_growth}
\alias{simulate_growth}
\title{Simulate Latent Growth-Curve Data}
\usage{
simulate_growth(
  n = 200,
  n_time = 5,
  intercept_mean = 0,
  slope_mean = 1,
  intercept_sd = 1,
  slope_sd = 0.5,
  intercept_slope_cor = 0,
  residual_sd = 1,
  seed = NULL
)
}
\arguments{
\item{n}{Integer. Number of subjects. Default: 200.}

\item{n_time}{Integer >= 2. Number of measurement occasions per subject.
Default: 5.}

\item{intercept_mean}{Numeric. Mean of subject intercepts \eqn{b_{0i}}.
Default: 0.}

\item{slope_mean}{Numeric. Mean of subject slopes \eqn{b_{1i}}. Default: 1.}

\item{intercept_sd}{Positive numeric. Standard deviation of subject
intercepts. Default: 1.}

\item{slope_sd}{Positive numeric. Standard deviation of subject slopes.
Default: 0.5.}

\item{intercept_slope_cor}{Numeric in \code{[-1, 1]}. Correlation between
subject intercepts and slopes. Default: 0.}

\item{residual_sd}{Positive numeric. Level-1 (occasion) residual standard
deviation \eqn{\sigma_e}. Default: 1.}

\item{seed}{Integer or NULL. Random seed.}
}
\value{
A \code{\link{saqr_sim}} object with:
\describe{
\item{\code{$data}}{long-format data.frame with columns \code{subject}
(factor), \code{time} (numeric, \code{0..n_time-1}), and \code{y}
(numeric).}
\item{\code{$params}}{list with \code{means} (intercept/slope),
\code{sds} (intercept/slope), \code{correlation}, \code{residual_sd},
\code{n_time}, \code{subject_intercepts} (true \eqn{b_{0i}}), and
\code{subject_slopes} (true \eqn{b_{1i}}).}
}
}
\description{
Generate longitudinal data from a linear latent growth-curve
model. Each subject draws a random intercept and slope from a bivariate
normal with specified means, standard deviations, and correlation, then
\deqn{y_{it} = b_{0i} + b_{1i} \, t + e_{it},}
with \code{time} coded \code{0..(n_time - 1)}. The result is returned in
long format. Designed so that the mean per-subject slope recovers
\code{slope_mean} and the spread of per-subject intercepts recovers
\code{intercept_sd} at large \code{n}.
}
\examples{
r <- simulate_growth(n = 300, n_time = 6, slope_mean = 2,
                     intercept_sd = 1.5, seed = 1)
print(r)
head(r)

# Correlated intercepts and slopes
r2 <- simulate_growth(n = 250, intercept_slope_cor = 0.5, seed = 42)
r2$params$correlation

}
