% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_processing.R
\name{batch_apply}
\alias{batch_apply}
\title{Apply Function to Multiple Models or Datasets}
\usage{
batch_apply(
  object_list,
  fun,
  parallel = FALSE,
  cores = NULL,
  progress = TRUE,
  simplify = FALSE,
  ...
)
}
\arguments{
\item{object_list}{A list of TNA model objects or data frames.}

\item{fun}{A function to apply to each element.}

\item{parallel}{Logical. Whether to use parallel processing. Default: FALSE.}

\item{cores}{Integer or NULL. Number of cores for parallel processing.
Default: NULL.}

\item{progress}{Logical. Whether to show progress messages. Default: TRUE.}

\item{simplify}{Logical. Whether to simplify results if possible.
Default: FALSE.}

\item{...}{Additional arguments passed to \code{fun}.}
}
\value{
A list of results from applying \code{fun} to each element.
If \code{simplify = TRUE} and results are atomic, returns a simplified vector.
}
\description{
Apply a function to a list of TNA models or datasets, with optional
parallel processing.
}
\details{
This is a general-purpose function for batch operations on model lists.
It handles errors gracefully, returning NULL for failed operations.
}
\examples{
\dontrun{
# Extract transition matrices from multiple models
trans_mats <- batch_apply(models, extract_transition_matrix)

# Compare each model to a reference
ref_model <- models[[1]]
comparisons <- batch_apply(
  models[-1],
  function(m) compare_networks(ref_model, m)$metrics$correlation
)

# Extract centralities with simplification
centralities <- batch_apply(
  models,
  function(m) tna::centralities(m),
  simplify = FALSE
)
}

}
\seealso{
\code{\link{batch_fit_models}} for fitting multiple models.
}
