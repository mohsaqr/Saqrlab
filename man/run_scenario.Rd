% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{run_scenario}
\alias{run_scenario}
\title{Run a Whole Scenario}
\usage{
run_scenario(scenario, seed = NULL)
}
\arguments{
\item{scenario}{Character scalar. The scenario name (see
\code{\link{list_scenarios}}).}

\item{seed}{Integer scalar or \code{NULL}. When supplied, each case is given
a deterministic seed of \code{seed + case index} so the whole run is
reproducible while every case still differs. When \code{NULL} the
simulators use their own defaults.}
}
\value{
A named list of \code{\link{saqr_sim}} objects, one per case, named
\code{case1}, \code{case2}, ...
}
\description{
Execute every case of a scenario by \code{do.call()}-ing its
argument-list against the scenario's simulator. Returns a named list of
the resulting \code{\link{saqr_sim}} objects, labelled \code{case1},
\code{case2}, ...
}
\examples{
s <- run_scenario("power_ttest", seed = 1)
s$case1

}
