% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_statistical.R
\name{simulate_correlation}
\alias{simulate_correlation}
\title{Simulate Correlated Multivariate Data}
\usage{
simulate_correlation(n, sigma, means = NULL, var_names = NULL, seed = NULL)
}
\arguments{
\item{n}{Integer. Sample size.}

\item{sigma}{Numeric matrix. Either a correlation matrix (all diagonal = 1)
or a covariance matrix. Must be symmetric and positive-definite (or
near-PD; corrected internally).}

\item{means}{Numeric vector or NULL. Population means for each variable.
Default: all zeros.}

\item{var_names}{Character vector or NULL. Variable names. Default:
\code{paste0("x", seq_len(ncol(sigma)))}.}

\item{seed}{Integer or NULL. Random seed.}
}
\value{
A \code{\link{saqr_sim}} object with:
\describe{
\item{\code{$data}}{data.frame with columns \code{x1}...\code{xp}.}
\item{\code{$params}}{list with \code{sigma} (the input matrix),
\code{means}, \code{is_correlation} (logical).}
}
}
\description{
Generate multivariate normal data from an explicit correlation
(or covariance) matrix. Designed so that \code{cor(r$data)} recovers
\code{sigma} at large \code{n}.
}
\examples{
# Correlation matrix
R <- matrix(c(1, 0.6, 0.3,
              0.6, 1, 0.5,
              0.3, 0.5, 1), nrow = 3)
r <- simulate_correlation(n = 200, sigma = R, seed = 1)
cor(r$data)           # should approximate R
r$params$sigma        # the true matrix

# With means and custom names
r2 <- simulate_correlation(n = 500, sigma = R,
                            means = c(10, 20, 30),
                            var_names = c("IQ", "GPA", "income"), seed = 42)

}
