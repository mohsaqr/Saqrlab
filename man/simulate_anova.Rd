% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_statistical.R
\name{simulate_anova}
\alias{simulate_anova}
\title{Simulate Multi-Group Comparison Data (ANOVA)}
\usage{
simulate_anova(n, means, sds = 1, labels = NULL, seed = NULL)
}
\arguments{
\item{n}{Integer or integer vector. If a single value, the per-group sample
size (equal groups). If a vector, must have length equal to
\code{length(means)} giving per-group sizes.}

\item{means}{Numeric vector. Population mean for each group.}

\item{sds}{Numeric scalar or vector. Standard deviation(s) for each group.
Scalar is recycled. Default: 1.}

\item{labels}{Character vector or NULL. Group labels. Default:
\code{paste0("G", seq_along(means))}.}

\item{seed}{Integer or NULL. Random seed.}
}
\value{
A \code{\link{saqr_sim}} object with:
\describe{
\item{\code{$data}}{data.frame with columns \code{group} (factor) and
\code{score} (numeric).}
\item{\code{$params}}{list with \code{means}, \code{sds} (as vector),
\code{n} (per-group sizes), \code{labels}, \code{eta_squared} (true
population \eqn{\eta^2}).}
}
}
\description{
Generate data for a one-way ANOVA with fully specified
group means and standard deviations. Designed so that
\code{aov(score ~ group, data = r$data)} recovers the true group effects
at large \code{n}.
}
\examples{
r <- simulate_anova(n = 30, means = c(10, 12, 15), seed = 1)
summary(aov(score ~ group, data = r$data))
r$params$eta_squared

# Unequal groups and heteroscedastic
r2 <- simulate_anova(n = c(50, 30, 20), means = c(5, 5, 10),
                      sds = c(1, 2, 3), seed = 42)

}
