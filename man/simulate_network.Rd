% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_network.R
\name{simulate_network}
\alias{simulate_network}
\title{Simulate statnet Network Object}
\usage{
simulate_network(
  n = NULL,
  model = c("er", "ba", "ws", "sbm", "reg", "grg", "ff"),
  name_source = c("human", "states"),
  regions = "all",
  categories = "all",
  names = NULL,
  directed = FALSE,
  weighted = FALSE,
  weights = c(0.1, 1),
  p = 0.1,
  m = NULL,
  power = 1,
  m_ba = 2,
  nei = 2,
  p_rewire = 0.05,
  blocks = 3,
  p_within = 0.3,
  p_between = 0.05,
  k = 4,
  radius = 0.25,
  fw = 0.35,
  bw = 0.32,
  seed = NULL
)
}
\arguments{
\item{n}{Integer or NULL. Number of nodes. If NULL (default), randomly
selects between 20-50 nodes.}

\item{model}{Character. Graph generation algorithm:
\itemize{
\item \code{"er"}: Erdos-Renyi random graph
\item \code{"ba"}: Barabasi-Albert scale-free network
\item \code{"ws"}: Watts-Strogatz small-world network
\item \code{"sbm"}: Stochastic Block Model (community structure)
\item \code{"reg"}: Regular graph (fixed degree)
\item \code{"grg"}: Geometric Random Graph (spatial)
\item \code{"ff"}: Forest Fire (growing network)
}
Default: "er".}

\item{name_source}{Character. Source for node names:
\itemize{
\item \code{"human"}: Culturally diverse human names from GLOBAL_NAMES
\item \code{"states"}: Learning action verbs from LEARNING_STATES
}
Default: "human".}

\item{regions}{Character vector. Regions to sample human names from (only used
when name_source = "human"). Can be specific regions (e.g., "arab", "east_asia"),
shortcuts (e.g., "europe", "africa", "asia"), or "all". See \code{\link{list_name_regions}}.
Default: "all".}

\item{categories}{Character vector. Learning state categories (only used when
name_source = "states"). Options: "metacognitive", "cognitive", "behavioral",
"social", "motivational", "affective", "group_regulation", "lms", or "all".
Default: "all".}

\item{names}{Character vector or NULL. Custom node names. Overrides
name_source if provided. Default: NULL.}

\item{directed}{Logical. If TRUE, generate directed network. Default: FALSE.}

\item{weighted}{Logical. If TRUE, add random edge weights. Default: FALSE.}

\item{weights}{Numeric vector of length 2. Weight range [min, max].
Default: c(0.1, 1.0).}

\item{p}{Numeric. Edge probability for Erdos-Renyi model. Default: 0.1.}

\item{m}{Integer or NULL. Fixed number of edges for Erdos-Renyi.
Overrides p if provided. Default: NULL.}

\item{power}{Numeric. Attachment power for Barabasi-Albert. Default: 1.}

\item{m_ba}{Integer. Edges per new vertex for Barabasi-Albert. Default: 2.}

\item{nei}{Integer. Neighborhood size for Watts-Strogatz. Default: 2.}

\item{p_rewire}{Numeric. Rewiring probability for Watts-Strogatz. Default: 0.05.}

\item{blocks}{Integer. Number of blocks for SBM. Default: 3.}

\item{p_within}{Numeric. Within-block edge probability for SBM. Default: 0.3.}

\item{p_between}{Numeric. Between-block edge probability for SBM. Default: 0.05.}

\item{k}{Integer. Degree for regular graphs. Default: 4.}

\item{radius}{Numeric. Connection radius for geometric random graph. Default: 0.25.}

\item{fw}{Numeric. Forward burning probability for Forest Fire. Default: 0.35.}

\item{bw}{Numeric. Backward burning factor for Forest Fire. Default: 0.32.}

\item{seed}{Integer or NULL. Random seed for reproducibility. Default: NULL.}
}
\value{
A network object (class "network") with vertex names and optional
edge weights.
}
\description{
Generate a statnet network object using common graph algorithms with
realistic node names from human names or learning states.
}
\details{
This function generates networks using igraph algorithms internally, then
converts to statnet's network class. The resulting object is compatible
with all sna and network package functions.

Vertex names are stored in the "vertex.names" attribute and can be accessed
with \code{network.vertex.names()}.
}
\examples{
\dontrun{
library(network)
library(sna)

# Default: Erdos-Renyi with human names
net <- simulate_network(n = 20, seed = 42)
class(net)  # "network"
network.vertex.names(net)  # Diverse human names

# Names from specific regions
net_asia <- simulate_network(n = 20, regions = "asia", seed = 42)
network.vertex.names(net_asia)  # Asian names

# Use learning state names instead
net_states <- simulate_network(n = 15, name_source = "states", seed = 42)
network.vertex.names(net_states)  # Action verbs

# Scale-free for SNA analysis
net_sf <- simulate_network(n = 50, model = "ba", seed = 42)
betweenness(net_sf)
closeness(net_sf)

# Community structure
net_sbm <- simulate_network(n = 30, model = "sbm", blocks = 3, seed = 42)

# Weighted network
net_w <- simulate_network(n = 20, model = "er", weighted = TRUE, seed = 42)
net_w \%e\% "weight"  # Edge weights
}

}
\seealso{
\code{\link{simulate_igraph}} for igraph objects,
\code{\link{simulate_matrix}} for adjacency matrices,
\code{\link{simulate_edge_list}} for edge list data frames.
}
