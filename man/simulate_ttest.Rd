% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_statistical.R
\name{simulate_ttest}
\alias{simulate_ttest}
\title{Simulate Two-Group Comparison Data (t-test)}
\usage{
simulate_ttest(
  n_a,
  n_b,
  mean_a,
  mean_b,
  sd_a = 1,
  sd_b = sd_a,
  labels = c("A", "B"),
  seed = NULL
)
}
\arguments{
\item{n_a}{Integer. Sample size for group A.}

\item{n_b}{Integer. Sample size for group B.}

\item{mean_a}{Numeric. Population mean for group A.}

\item{mean_b}{Numeric. Population mean for group B.}

\item{sd_a}{Positive numeric. Standard deviation for group A. Default: 1.}

\item{sd_b}{Positive numeric. Standard deviation for group B. Default:
same as \code{sd_a}.}

\item{labels}{Character vector of length 2. Group labels. Default:
\code{c("A", "B")}.}

\item{seed}{Integer or NULL. Random seed.}
}
\value{
A \code{\link{saqr_sim}} object with:
\describe{
\item{\code{$data}}{data.frame with columns \code{group} (factor) and
\code{score} (numeric).}
\item{\code{$params}}{list with \code{mean_a}, \code{mean_b}, \code{sd_a},
\code{sd_b}, \code{n_a}, \code{n_b}, \code{cohens_d} (true Cohen's d
based on pooled SD).}
}
}
\description{
Generate data for a two-group comparison with fully specified
ground-truth means and standard deviations. Designed so that
\code{t.test(score ~ group, data = r$data)} recovers the true difference
at large \code{n}.
}
\examples{
r <- simulate_ttest(n_a = 50, n_b = 50, mean_a = 100, mean_b = 105, seed = 1)
t.test(score ~ group, data = r$data)
r$params$cohens_d

}
