% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_probabilities.R
\name{generate_probabilities}
\alias{generate_probabilities}
\title{Generate Transition and Initial Probabilities}
\usage{
generate_probabilities(
  n_states = 8,
  states = NULL,
  alpha = 1,
  diag_c = 0,
  seed = NULL,
  possible_state_names = NULL
)
}
\arguments{
\item{n_states}{Integer. Number of states in the Markov chain. Default: 5.}

\item{states}{Character vector. Names for the states.
Must have at least \code{n_states} elements. If NULL, uses letters A, B, C, ...
Default: NULL.}

\item{alpha}{Numeric. Dirichlet concentration parameter passed to
\code{\link[seqHMM:simulate_pars]{seqHMM::simulate_initial_probs()}} and
\code{\link[seqHMM:simulate_pars]{seqHMM::simulate_transition_probs()}}.
Small values (e.g., 0.1) produce sparse matrices with a few dominant
transitions; large values (e.g., 10) produce near-uniform matrices.
Default: 1.}

\item{diag_c}{Numeric. Diagonal boost added before row normalisation,
passed to \code{\link[seqHMM:simulate_pars]{seqHMM::simulate_transition_probs()}}.
Higher values create "sticky" states with strong self-transitions.
Default: 0.}

\item{seed}{Integer or NULL. Random seed for reproducibility. Default: NULL.}

\item{possible_state_names}{Deprecated. Use \code{states} instead.}
}
\value{
A list containing:
\describe{
\item{initial_probs}{Named numeric vector of initial state probabilities.}
\item{transition_probs}{Square matrix of transition probabilities with
row and column names set to state names.}
\item{state_names}{Character vector of state names used.}
}
}
\description{
Generate random transition probabilities and initial state probabilities
for a Markov chain using the seqHMM package.
}
\details{
The transition probabilities are generated using
\code{seqHMM::simulate_transition_probs()} and initial probabilities using
\code{seqHMM::simulate_initial_probs()}.
}
\examples{
\dontrun{
# Generate probabilities for 4 states with default names
probs <- generate_probabilities(n_states = 4, seed = 42)

# Generate probabilities with custom state names
probs <- generate_probabilities(
  n_states = 4,
  states = c("A", "B", "C", "D", "E"),
  seed = 123
)

# View initial probabilities
probs$initial_probs

# View transition matrix
probs$transition_probs

# Old parameter name still works
probs <- generate_probabilities(
  n_states = 3,
  possible_state_names = c("X", "Y", "Z")
)
}

}
